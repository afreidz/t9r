{
  "name": "t9r",
  "type": "module",
  "scripts": {
    "swa": "swa start",
    "build": "turbo run build --color",
    "check": "turbo run check --color",
    "dev": "turbo run dev swa --color",
    "prepare": "husky install"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "devDependencies": {
    "husky": "^8.0.3",
    "turbo": "^1.8.3"
  }
}
