{
  "name": "t9r",
  "type": "module",
  "scripts": {
    "swa": "swa start",
    "build": "turbo run build --color",
    "check": "turbo run check --color",
    "dev": "turbo run watch dev swa --color",
    "prepare": "husky install"
  },
  "engines": {
    "node": "18"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "devDependencies": {
    "husky": "^8.0.3",
    "turbo": "^1.9.3"
  }
}
