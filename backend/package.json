{
  "name": "t9r-backend",
  "type": "module",
  "scripts": {
    "build": "esbuild --bundle function/index.ts --outfile=dist/function.cjs --platform=node --target=node16",
    "watch": "npm run build -- --watch",
    "dev": "func start --port 20232",
    "check": "prettier ./ --check && tsc --noEmit ./function/index.ts"
  },
  "devDependencies": {
    "@azure/functions": "^3.0.0",
    "@total-typescript/ts-reset": "^0.3.7",
    "@trpc/server": "^10.12.0",
    "@types/node": "16.x",
    "azure-functions-core-tools": "^4.x",
    "esbuild": "0.17.10",
    "esbuild-plugin-alias": "^0.2.1",
    "mongodb": "^5.1.0",
    "prettier": "^2.8.4",
    "temporal-polyfill": "^0.1.1",
    "typescript": "^4.0.0",
    "zod": "^3.20.6"
  }
}
